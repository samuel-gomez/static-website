{
  "name": "@wooweb/design-system",
  "version": "1.1.3",
  "description": "",
  "main": "index.js",
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "bootstrap": "lerna bootstrap",
    "publish": "lerna publish --yes --concurrency 1 --force-publish",
    "create": "lerna create",
    "add": "lerna add",
    "clean": "lerna clean",
    "remove": "lerna exec -- npm remove"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "lerna": "^3.13.4",
    "vanilla-tilt": "^1.7.0"
  },
  "devDependencies": {
    "jest": "^24.8.0"
  }
}
