mixin ArticleText({ className = 'article-text', modifier = '', js=false, title, text, id='', link= {}, image= {} })
    -const classComponent = setClass(className, modifier, js)
    article(class=classComponent)
        if image.name !== undefined
            +Img({src:`${basedir}${pathImg}${image.name}`, alt: image.alt, modifier, className: `${prefix}-${className}__img` })
        div(class=setClass(`${className}__content`, modifier))
            h2(class=setClass(`${className}__title`, modifier))
                a(href= link.href title=title)
                    | !{title}
            span(class=setClass(`${className}__intro`, modifier)) 
                | !{text}
            if link.href !== undefined && link.label !== undefined
                +Link({ label: link.label, href: link.href, title: link.title, modifier })
            block